/* SEMA SSO Docs — English translations */
window.T = {
  en: {
    /* sidebar / nav */
    'sidebar.title': 'Sema Universe',
    'sidebar.subtitle': 'SSO Integration Docs',
    'sidebar.footer.version': 'Sema Universe v1.0',
    'sidebar.footer.org': 'Internal — Kaeyros Analytics',
    'nav.section.intro': 'Introduction',
    'nav.section.integration': 'Integration',
    'nav.section.reference': 'Reference',
    'nav.overview': 'Overview & Architecture',
    'nav.keycloak': 'Keycloak Setup',
    'nav.gateway': 'SSO API Gateway',
    'nav.frontend': 'Frontend Apps',
    'nav.backend': 'Backend Services',
    'nav.router': 'sema-router (Go)',
    'nav.envvars': 'Environment Variables',
    'nav.newapp': 'New App Guide',
    'nav.tokenflow': 'Token Flow Diagrams',

    /* topbar */
    'topbar.env': '● Active',

    /* overview */
    'page.overview.title': 'Overview & Architecture',
    'page.overview.desc': 'How authentication works across all Sema Universe applications — and how every app shares a single identity.',

    /* overview — intro + glossary */
    'overview.about.title': 'About This Guide',
    'overview.about.body': 'This document is written for developers who build, integrate, or maintain applications in the Sema Universe. It explains how authentication works end-to-end: how a user logs in, how that login session travels through the system, and what each service must do to validate it. If you are new to the project, start here — read the glossary below before diving into the technical sections.',
    'overview.about.tip': 'Not familiar with terms like JWT, OIDC, or HMAC? Read the glossary below first. Everything else in this document builds on those concepts.',

    'card.glossary.title': 'Glossary — Key Terms',
    'card.glossary.desc': 'Plain-language definitions for every technical term used in this document. Read these first if you are new to the project.',

    'glossary.sso.fullname': 'Single Sign-On',
    'glossary.sso.def': 'A user logs in <em>once</em> and gets access to all Sema applications — core, order, bulk, and more — without being asked to log in again. One identity shared across the entire platform. The SSO API Gateway makes this possible by acting as the single authentication point for all apps.',

    'glossary.keycloak.def': 'The open-source identity server that stores user accounts and displays the actual login form. Think of it as the "user database with a login page." Sema runs one Keycloak instance with a realm called <code>SEMA-UNIVERSE</code>. <strong>No application ever contacts Keycloak directly</strong> — they all go through the API Gateway.',

    'glossary.gateway.term': 'API Gateway',
    'glossary.gateway.def': 'The <code>SEMA-SSO-api-gateway</code> service is the single authentication broker for the entire platform. When a user needs to log in, every app redirects to the gateway. The gateway handles the full OIDC flow with Keycloak, then issues its own short-lived JWT back to the application. This means only one service ever needs to know how to talk to Keycloak.',

    'glossary.jwt.fullname': 'JSON Web Token',
    'glossary.jwt.def': 'A compact, digitally-signed string that proves who you are. After a successful login, the gateway mints a JWT containing the user\'s identity and permissions. This token is attached to every API request via the <code>Authorization: Bearer</code> header. It expires after 1 hour, after which the client must use a refresh token to get a new one.',

    'glossary.oidc.fullname': 'OpenID Connect',
    'glossary.oidc.def': 'The industry-standard protocol the gateway uses to communicate with Keycloak. It is built on top of OAuth 2.0 and adds a standardised way to share user identity information. You do not need to implement OIDC yourself — the gateway handles the entire flow on behalf of every application.',

    'glossary.redis.def': 'An in-memory data store used by the gateway for two purposes: <strong>(1) session storage</strong> — it keeps track of active login sessions, and <strong>(2) token blacklisting</strong> — when a user logs out, the token\'s unique ID (<code>jti</code>) is written to Redis so all services immediately stop accepting it. Without Redis, logout would not work across services.',

    'glossary.hmac.fullname': 'Hash-based Message Authentication Code',
    'glossary.hmac.def': 'A cryptographic signature the gateway adds to every request it forwards to a backend service. Computed as <code>HMAC-SHA256("{timestamp}:{method}:{path}", GATEWAY_SHARED_SECRET)</code>. Backend services verify this signature to confirm the request genuinely came from the gateway and was not tampered with in transit.',

    'card.core_principle.title': 'The Core Principle',
    'card.core_principle.body': 'Every Sema application — frontend and backend — trusts only one source of authentication: the <strong>SSO API Gateway</strong>. No application talks to Keycloak directly. After a user logs in through Keycloak, the gateway issues its own JWT signed with a secret shared with all backend services. This architecture means you can swap out, update, or reconfigure Keycloak without touching a single application — only the gateway changes.',
    'card.core_principle.insight': '<strong>Why does this matter?</strong> Backend services never see Keycloak tokens. They only see gateway-issued JWTs. If Keycloak is replaced, upgraded, or its configuration changes, only the gateway needs to be updated. All other services remain completely unaffected.',
    'card.arch.title': 'System Architecture',
    'arch.user': 'User',
    'arch.user.sub': 'Browser',
    'arch.frontend': 'Frontend App',
    'arch.frontend.sub': 'Next.js / React',
    'arch.gateway': 'SSO Gateway',
    'arch.gateway.sub': 'Express + Redis',
    'arch.keycloak': 'Keycloak',
    'arch.keycloak.sub': 'SEMA-UNIVERSE',
    'arch.after': '↓ After login: gateway issues its own JWT',
    'arch.jwt': 'Gateway JWT',
    'arch.frontend2.sub': 'Authorization: Bearer &lt;token&gt;',
    'arch.backend.sub': 'JWT Strategy + Redis JTI',
    'arch.router.sub': 'Fetches credentials by phone_number_id',
    'card.apps.title': 'Apps in the Universe',
    'app.core.name': 'sema-core-frontend',
    'app.core.role': 'Main operations dashboard',
    'app.order.name': 'sema-order-frontend',
    'app.order.role': 'Order management interface',
    'app.sso.name': 'sema-sso-frontend',
    'app.sso.role': 'SSO login & onboarding',
    'app.bulk.name': 'sema-bulk-frontend',
    'app.bulk.role': 'Bulk messaging campaigns',
    'card.apps.backends': 'Backend Services',
    'card.apps.infra': 'Infrastructure',
    'card.flow.title': 'Authentication Flow Summary',
    'flow.1.title': 'User visits any frontend app',
    'flow.1.desc': 'Frontend checks <code>/auth/check</code> on the gateway. If <code>authenticated: false</code>, redirects to <code>/auth/login</code>.',
    'flow.2.title': 'Gateway initiates OIDC flow',
    'flow.2.desc': 'Redirects browser to Keycloak (<code>SEMA-UNIVERSE</code> realm, <code>api-gateway</code> client) with PKCE + state.',
    'flow.3.title': 'User authenticates on Keycloak',
    'flow.3.desc': 'Keycloak handles login UI (using custom SEMA theme). Issues authorization code to gateway\'s callback URI.',
    'flow.4.title': 'Gateway exchanges code for tokens',
    'flow.4.desc': '<code>GET /auth/callback</code> exchanges code with Keycloak. Gateway mints its own JWT (<code>iss: "api-gateway"</code>) signed with <code>GATEWAY_TOKEN_SECRET</code>, stores session in Redis.',
    'flow.5.title': 'Frontend receives JWT via redirect_uri',
    'flow.5.desc': 'With <code>callback_mode=direct</code>, gateway redirects to <code>redirect_uri?token=&lt;jwt&gt;</code>. Frontend extracts token from the URL query param, stores it, and calls <code>/auth/session</code> + <code>/auth/profile</code>.',
    'flow.6.title': 'API calls use gateway JWT',
    'flow.6.desc': 'Every request includes <code>Authorization: Bearer &lt;gateway_token&gt;</code>. Backend services validate issuer, audience, and JTI against Redis blacklist. Gateway middleware also signs requests with HMAC-SHA256 (<code>x-gateway-signature</code>).',

    /* arch new keys */
    'arch.conn.visit': 'visits app',
    'arch.conn.oidc': 'OIDC code flow',
    'arch.conn.jwt': 'JWT + HMAC-SHA256',
    'arch.redis.sub': 'Sessions + JTI',
    'arch.return': 'Gateway redirects back to the originating app: <code>redirect_uri?token=&lt;jwt&gt;</code> — each app receives its own JWT without sharing an auth page with other apps.',
    'arch.gateway.sub': 'Express + Redis — Port 3001',

    /* keycloak */
    'page.keycloak.title': 'Keycloak',
    'page.keycloak.desc': 'The SEMA-UNIVERSE realm and <code>api-gateway</code> client already exist and are shared by the entire platform. Your new app does not talk to Keycloak directly — the SSO gateway handles all OIDC on behalf of every app.',
    'kc.tab.local': 'Local Development',
    'kc.tab.deployed': 'Deployed Environments',
    'card.kc.local.ref.title': 'Local Instance Reference',
    'card.kc.deployed.title': 'Deployed Keycloak Admin',
    'kc.deployed.desc': 'The deployed Keycloak instance is managed infrastructure — no Docker needed. Access the admin console in your browser. Each environment has its own dedicated realm: use <code>SEMA-UNIVERSE-DEV</code> for development, <code>SEMA-UNIVERSE-STG</code> for staging, and <code>SEMA-UNIVERSE</code> for production.',
    'kc.deployed.realm.note': 'Always work in the realm that matches your deployment target. Redirect URIs registered in the DEV realm only apply to the DEV environment — register in each realm as needed.',
    'card.kc.existing.title': 'Existing Infrastructure — Do Not Recreate',
    'kc.existing.warn': 'Do <strong>not</strong> create a new realm or a new Keycloak client. The realm <code>SEMA-UNIVERSE</code> and the client <code>api-gateway</code> are shared by every app in Sema Universe. To integrate a new frontend app, no Keycloak changes are needed at all — your app only needs to call the SSO gateway\'s <code>/auth/login</code> endpoint.',
    'card.kc.nocontact.title': 'Your App Never Calls Keycloak Directly',
    'kc.nocontact.desc': 'The SSO gateway (<code>SEMA-SSO-api-gateway</code>) is the <em>only</em> service that communicates with Keycloak. When a user needs to log in, your frontend redirects to the gateway — not to Keycloak. The gateway then runs the full OIDC Authorization Code flow with Keycloak internally, mints its own JWT, and redirects the token back to your app via <code>redirect_uri?token=&lt;jwt&gt;</code>.',
    'kc.nocontact.tip': 'You do not need <code>KEYCLOAK_*</code> environment variables in your new app. Those belong only in the gateway\'s <code>.env</code>. Your app only needs <code>NEXT_PUBLIC_SEMA_SSO_URL</code> pointing to the gateway.',
    'card.kc.launch.title': 'Start Keycloak Locally',
    'kc.launch.desc': 'Keycloak runs via Docker Compose from the <code>SEMA-KEYCLOAK-THEME</code> repository. The realm configuration is auto-imported from <code>realm-import.json</code> on first start; if it already exists in the Docker volume, the import is skipped automatically. Data persists across restarts thanks to a named Docker volume (<code>keycloak_data</code>).',
    'kc.launch.restart.label': 'Start Keycloak (must run from the <code>SEMA-KEYCLOAK-THEME</code> directory):',
    'kc.launch.firsttime.label': 'Or run detached in the background:',
    'kc.launch.warn': '<strong>Development only.</strong> The <code>start-dev</code> mode disables TLS and CORS restrictions — do not use in production. Admin credentials: <code>admin</code> / <code>admin</code>. The gateway\'s <code>KEYCLOAK_REALM</code> env var controls which realm is used per environment.',
    'card.kc.addredirect.title': 'Add Your App\'s Redirect URI (New Port Only)',
    'kc.addredirect.desc': 'The existing <code>api-gateway</code> client in Keycloak only allows redirects to ports it knows about. If your new frontend runs on a port not already listed, you must add it. This is a one-time step per new port — you do not touch any other Keycloak setting.',
    'kc.addredirect.step.1': 'Open the Keycloak admin console at <strong>http://localhost:8081</strong> → select realm <strong>SEMA-UNIVERSE</strong> → click <strong>Clients</strong> → open <strong>api-gateway</strong> → go to the <strong>Settings</strong> tab.',
    'kc.addredirect.step.2': 'Add the following two entries (replace <code>YOUR_PORT</code> with your app\'s actual port, e.g. <code>3008</code>). Web Origins is already set to <code>*</code> — no change needed there:',
    'kc.addredirect.step.3': 'Click <strong>Save</strong> at the bottom of the page. No gateway restart needed.',
    'ss.label': 'Screenshot',
    'ss.settings': 'api-gateway client → Settings tab → Valid redirect URIs → add your port → Save',
    'card.kc.secret.title': 'Get the Client Secret (Gateway .env Only)',
    'kc.secret.intro': 'The client secret is already configured in the running gateway\'s <code>.env</code> as <code>KEYCLOAK_CLIENT_SECRET</code>. You only need this step if you are setting up the SSO gateway itself on a new machine.',
    'kc.secret.step.a': 'Open the Keycloak admin at <strong>http://localhost:8081</strong> → <strong>SEMA-UNIVERSE</strong> → <strong>Clients</strong> → <strong>api-gateway</strong>.',
    'kc.secret.step.b': 'Click the <strong>Credentials</strong> tab. Click the eye icon next to <strong>Client secret</strong> to reveal it, then copy the value.',
    'kc.secret.step.c': 'Paste it into the gateway\'s <code>.env</code>:',
    'ss.secret': 'api-gateway client → Credentials tab → Client secret → eye icon → Copy',
    'card.kc.services.title': 'All Local Service Launch Commands',
    'kc.services.desc': 'Start each service in a separate terminal in this order: Keycloak first (gateway reads its discovery document on startup), then gateway, then backends, then frontends.',
    'kc.services.tip': 'Keycloak must be running before you start the gateway. The gateway connects to Keycloak\'s OIDC discovery endpoint on startup and will fail if Keycloak is not reachable.',
    'card.kc.config.title': 'Realm Configuration Reference',
    'th.setting': 'Setting',
    'th.value': 'Value',
    'kc.realm': 'Realm Name',
    'kc.client': 'Client ID',
    'kc.url': 'Base URL',
    'kc.redirect': 'Valid Redirect URIs',
    'kc.grant': 'Grant Type',
    'kc.auth': 'Token Endpoint Auth',
    'kc.theme': 'Theme',
    'card.kc.oidc.title': 'OIDC Discovery',
    'kc.oidc.desc': 'Keycloak exposes a standard OIDC discovery document at:',
    'kc.oidc.note': 'The gateway uses the token endpoint, authorization endpoint, and JWKS URI from this document.',
    'card.kc.checklist.title': 'Client Setup Checklist',
    'kc.check.1': '<strong>Client Type:</strong> Confidential (not public)',
    'kc.check.2': '<strong>Standard Flow:</strong> Enabled (Authorization Code)',
    'kc.check.3': '<strong>Direct Access Grants:</strong> Disabled in production',
    'kc.check.4': '<strong>Valid Redirect URIs:</strong> Must include the gateway callback URL exactly',
    'kc.check.5': '<strong>Client Secret:</strong> Copy to <code>KEYCLOAK_CLIENT_SECRET</code> in gateway <code>.env</code>',
    'kc.check.6': '<strong>Audience mapper:</strong> Add <code>api-gateway</code> to the access token audience',
    /* keycloak — import configuration */
    'card.kc.import.title': 'Import Keycloak Configuration',
    'kc.import.desc': 'On a fresh machine, the SEMA-UNIVERSE realm does not exist yet — Docker starts Keycloak but does not create the realm automatically. Download both files below and import them into your local Keycloak to get a fully configured instance in minutes.',
    'kc.import.tip': '<strong>Why two files?</strong> <code>realm-export.json</code> creates the entire SEMA-UNIVERSE realm: clients, scopes, authentication flows, and the login theme. <code>api-gateway.json</code> restores the <code>api-gateway</code> client with the exact shared dev secret your gateway <code>.env</code> needs — Keycloak masks secrets in realm exports, so the separate client file is required to set the known value.',
    'kc.import.download.realm.label': 'Download realm-export.json',
    'kc.import.download.client.label': 'Download api-gateway.json',
    'kc.import.step.1': 'Open <strong>http://localhost:8081</strong> in your browser and log in with <code>admin</code> / <code>admin</code>. Make sure Keycloak is already running via <code>docker compose up</code> before this step.',
    'kc.import.step.2': 'Create the realm — click the realm dropdown in the top-left corner (shows <strong>Keycloak</strong> or <strong>master</strong>) → <strong>Create realm</strong>. On the form that appears, click <strong>Browse…</strong> next to "Realm file" → select <strong>realm-export.json</strong> → click <strong>Import</strong>. The realm name (<code>SEMA-UNIVERSE</code>) fills in automatically.',
    'kc.import.step.3': 'Verify the import — <strong>SEMA-UNIVERSE</strong> now appears in the realm selector (top-left). Click it to switch into the realm. Confirm the Clients list includes <code>api-gateway</code> and that authentication flows and client scopes are all present.',
    'kc.import.step.4': 'Import the api-gateway client — inside <strong>SEMA-UNIVERSE</strong> realm → click <strong>Clients</strong> in the left sidebar → click <strong>Import client</strong> (button in the top-right area of the Clients list page) → select <strong>api-gateway.json</strong> → click <strong>Save</strong>. This sets the correct client secret that the gateway expects.',
    'kc.import.step.5': 'Configure the gateway — paste the line below into <code>SEMA-SSO-api-gateway/.env</code>. This is the shared local dev secret from the file you just imported.',
    'kc.import.secret.warn': '<strong>Local development only.</strong> This secret is for local Keycloak instances only. Staging and production environments use different secrets managed by the infrastructure team.',
    'kc.secret.label': 'Client Secret',
    /* keycloak — deployed gateway callback */
    'card.kc.gw.callback.title': 'Gateway Callback URL in Keycloak',
    'kc.gw.callback.desc': 'Only the SSO gateway\'s own callback URL is registered in Keycloak — not individual app redirect URIs. Frontend apps register their <code>redirect_uri</code> with the gateway (via the gateway\'s allowed list), not directly in Keycloak. Only add a new entry here if you are deploying the gateway itself to a new domain or environment.',
    'kc.gw.callback.step.1': 'Open the admin console for the target environment (see URLs above) → select the target realm → <strong>Clients</strong> → open <strong>api-gateway</strong> → go to the <strong>Settings</strong> tab.',
    'kc.gw.callback.step.2': 'Under <strong>Valid Redirect URIs</strong>, add the gateway\'s callback URL for this new environment. This is the <strong>gateway\'s</strong> domain — not your frontend app\'s domain:',
    'kc.gw.callback.step.3': 'Click <strong>Save</strong> at the bottom of the page. No gateway restart required.',
    'card.kc.theme.title': 'Keycloak Theme',
    'kc.theme.desc': 'The <code>SEMA-KEYCLOAK-THEME</code> repo contains a custom Keycloak login theme with Sema branding. Deploy it by copying the theme folder to the Keycloak themes directory and selecting it in the realm settings.',
    'kc.theme.tip': 'For local development, the default Keycloak theme works fine. The custom theme is only needed in staging/production.',

    /* gateway */
    'page.gateway.title': 'SSO API Gateway',
    'page.gateway.desc': 'The central authentication broker — issues tokens, manages sessions, and proxies identity to all services.',
    'card.gw.sharedsecret.title': 'Critical: Shared Secrets Synchronization',
    'gw.sharedsecret.danger': '<strong>If any secret mismatches, 100% of API calls will fail with 401 Unauthorized.</strong> These must be copy-pasted exactly — no leading/trailing spaces, no regeneration without updating all services.',
    'th.variable': 'Variable',
    'th.where': 'Required In',
    'gw.secret.token.where': 'SSO API Gateway + ALL NestJS backends',
    'gw.secret.shared.where': 'SSO API Gateway + ALL NestJS backends',
    'gw.sharedsecret.how': 'Generate once with Node.js — then copy the same value into every <code>.env</code>:',
    'gw.hmac.gateway.desc': 'This code runs inside the gateway (Express) when forwarding any request to a backend service. The signature is a HMAC-SHA256 of the Unix timestamp using the shared secret.',
    'gw.hmac.backend.desc': 'This NestJS middleware runs on every incoming request. It rejects requests older than 5 minutes (replay attack prevention) and validates the HMAC signature.',
    'gw.hmac.warn': '<strong>Apply globally.</strong> Register <code>GatewayMiddleware</code> in <code>AppModule.configure()</code> for all routes. Any route without this check can be called by bypassing the gateway.',
    'card.gw.endpoints.title': 'Auth Endpoints',
    'th.method': 'Method',
    'th.path': 'Path',
    'th.description': 'Description',
    'gw.ep.login.desc': 'Initiates OAuth2/OIDC. Redirects browser to Keycloak. Accepts <code>redirect_uri</code> and <code>callback_mode=direct</code> query params.',
    'gw.ep.callback.desc': 'Keycloak redirects here after login. Exchanges code for tokens, mints gateway JWT, creates Redis session, then redirects to <code>redirect_uri</code>.',
    'gw.ep.session.desc': 'Returns current user + gateway token. Called by frontend on mount.',
    'gw.ep.check.desc': 'Returns <code>{ authenticated: boolean }</code>. Lightweight check used before API calls.',
    'gw.ep.profile.desc': 'Returns full Keycloak user profile (name, email, roles, etc.).',
    'gw.ep.refresh.desc': 'Exchanges refresh token for a new access token. Called automatically by <code>httpClient</code> on 401.',
    'gw.ep.logout.desc': 'Destroys Redis session, blacklists JTI, redirects to Keycloak logout. Accepts <code>redirect_uri</code>.',
    'card.gw.jwt.title': 'Gateway JWT Structure',
    'gw.jwt.desc': 'The gateway issues its own JWT — NOT the Keycloak token. Backend services only trust this token.',
    'gw.jwt.warn': '<strong>Important:</strong> The token is signed with a <em>symmetric</em> <code>GATEWAY_TOKEN_SECRET</code> — NOT JWKS. Both the gateway and all backend services must share the exact same secret value.',
    'card.gw.hmac.title': 'Service-to-Service Auth (HMAC)',
    'gw.hmac.desc': 'When the gateway forwards requests to backend services on behalf of the frontend, it adds two extra headers for request integrity verification.',
    'th.header': 'Header',
    'card.gw.routes.title': 'Route Registration — Adding a New Backend',
    'gw.routes.desc': 'Every backend service must be declared in the gateway\'s route files before it can receive forwarded requests. There are <strong>four files</strong> to update — one for local development and one per deployed environment.',
    'gw.routes.warn': 'Without a matching entry in these files the gateway returns <code>404 Not Found</code> for every request to that service, regardless of authentication state.',
    'th.file': 'File',
    'th.used': 'Used when',
    'gw.routes.local.when': 'Local development — <code>bun run start:dev</code>',
    'gw.routes.dev.when': 'Dev environment (CI/CD pipeline)',
    'gw.routes.stg.when': 'Staging environment (CI/CD pipeline)',
    'gw.routes.prod.when': 'Production environment (CI/CD pipeline)',
    'gw.routes.local.desc': 'Add an entry for your service in <code>routes.yml</code> (repo root). The <code>path</code> is the URL prefix clients use; <code>target</code> is where the gateway forwards the request.',
    'gw.routes.deployed.desc': 'For each deployed environment, add the same entry pointing to the deployed URL. Repeat for <code>routes-stg.yml</code> and <code>routes-prod.yml</code>.',
    'gw.routes.service-id.info': 'The <code>path</code> value in routes.yml <strong>must equal</strong> the <code>SERVICE_ID</code> environment variable in your backend service. The gateway uses it to build the JWT <code>aud</code> claim and verify the token is destined for your service.',
    'card.gw.session.title': 'Session Storage',
    'gw.session.desc': 'The gateway uses <strong>Redis</strong> for two purposes:',
    'gw.session.store.title': 'Session Store',
    'gw.session.store.desc': 'Stores Express sessions keyed by <code>SESSION_SECRET</code>. Cross-subdomain support via <code>SESSION_DOMAIN</code>. Used for <code>/auth/session</code> and callback state.',
    'gw.session.jti.title': 'JTI Blacklist',
    'gw.session.jti.desc': 'On logout, the JWT\'s <code>jti</code> is written to Redis with TTL = token expiry. All NestJS services check this on every request to block revoked tokens.',

    /* frontend */
    'page.frontend.title': 'Frontend Apps',
    'page.frontend.desc': 'All four Next.js apps follow the same pattern. Pick an app below to see its specific configuration.',
    'fe.code.middleware.desc': 'Next.js middleware runs on every request before rendering. This file protects all non-public routes by checking the presence of the auth token in cookies. Unauthenticated requests are redirected to the login page.',
    'fe.code.authprovider.desc': '<code>AuthProvider</code> fetches the session and profile from the SSO gateway on mount, stores them in React Query cache, and exposes them through the <code>useAuth()</code> hook. The <code>export function useAuth()</code> declaration is at the bottom of this file.',
    'fe.code.httpclient.desc': 'A pre-configured Axios instance that automatically attaches the <code>Authorization: Bearer &lt;token&gt;</code> header and the <code>x-credential-id</code> header on every request. The 401 interceptor triggers a silent token refresh before retrying. Always use this — never use <code>fetch</code> directly for authenticated endpoints.',
    'fe.code.login.desc': 'On render, the login page checks for a <code>?token=</code> query param (set by the gateway after redirect). If found, it stores the token and redirects to the dashboard. The login button redirects to the gateway with <code>redirect_uri</code> and <code>callback_mode=direct</code> so the token comes back to this app directly.',
    'tab.core': 'sema-core',
    'tab.order': 'sema-order',
    'tab.sso': 'sema-sso',
    'tab.bulk': 'sema-bulk',
    'card.fe.common.title': 'Common Frontend Pattern',
    'fe.env.label': 'Environment',
    'fe.providers.label': 'Providers',
    'fe.providers.desc': 'Every app wraps its root layout with <code>&lt;AuthProvider&gt;</code>. This mounts the <code>useAuth()</code> hook via React Query, which fetches <code>/auth/session</code> and <code>/auth/profile</code> on load.',
    'fe.keyfiles': 'Key auth files',
    'fe.postlogin': 'Post-login redirect',
    'fe.backend': 'Backend target',
    'fe.credential': 'Credential scoping',
    'fe.same': 'Same pattern',
    'fe.core.postlogin': 'After successful login, redirects to <code>/{locale}/home</code>',
    'fe.core.backend': '<code>sema-core-service</code> via <code>NEXT_PUBLIC_API_URL</code>',
    'fe.core.credential': 'Passes <code>x-credential-id</code> header with every API call for multi-tenant isolation',
    'fe.order.postlogin': 'After successful login, redirects to <code>/dashboard</code>',
    'fe.order.backend': '<code>sema-order-backend</code> via <code>NEXT_PUBLIC_API_URL</code>',
    'fe.order.same': 'Uses identical <code>AuthProvider</code> + <code>useAuth()</code> + <code>httpClient</code> pattern as sema-core-frontend.',
    'fe.sso.postlogin': 'After successful login, redirects to <code>/account</code>',
    'fe.sso.backend': '<code>SEMA-SSO-api-gateway</code> for identity ops, plus <code>sema-core-service</code> for credential management',
    'fe.sso.special': '<strong>Special role:</strong> This is the identity management app — it handles account creation, onboarding new WhatsApp numbers, and managing credentials. It still authenticates via the same SSO gateway.',
    'fe.sso.facebook': 'Also initiates a <strong>separate</strong> Facebook Login flow during WhatsApp onboarding to obtain the WABA access token. Required scopes include <code>whatsapp_business_messaging</code>, <code>business_management</code>, <code>catalog_management</code>.',
    'fe.sso.facebook.label': 'Facebook OAuth',
    'fe.bulk.postlogin': 'After successful login, redirects to <code>/dashboard</code>',
    'fe.bulk.backend': '<code>sema-bulk-backend</code> — which auto-provisions a <code>BulkTenant</code> on first request using <code>x-credential-id</code>',
    'fe.bulk.auto.label': 'Auto-provisioning',
    'fe.bulk.auto.desc': 'No manual tenant creation needed. The bulk backend creates the tenant automatically on first API call per credential.',

    /* backend */
    'page.backend.title': 'Backend Services (NestJS)',
    'page.backend.desc': 'Three NestJS services — all validate the same gateway JWT, all require the same two secrets.',
    'be.code.module.desc': 'Register <code>JwtModule</code> with the shared secret and configure <code>GatewayMiddleware</code> globally. The middleware runs before every controller — no route should be reachable without passing gateway signature verification.',
    'be.code.jwtstrategy.desc': '<code>JwtStrategy</code> validates three things in order: (1) issuer must be <code>"api-gateway"</code> — rejects tokens from Keycloak or any other source; (2) audience must include this service\'s <code>SERVICE_ID</code> — rejects tokens meant for other services; (3) JTI must not be in the Redis blacklist — rejects tokens invalidated by logout.',
    'be.code.cred.desc': 'Controllers extract <code>x-credential-id</code> from request headers to scope all database queries to the correct WhatsApp credential. This is how multi-tenancy is enforced at the API level.',
    'card.be.jwt.title': 'JWT Validation Strategy',
    'be.jwt.desc': 'Every NestJS backend implements a <code>JwtStrategy</code> (Passport.js) that validates:',
    'card.be.cred.title': 'Credential Header',
    'be.cred.desc': 'Multi-tenancy is implemented via the <code>x-credential-id</code> header. This is set by the frontend on every request and identifies which WhatsApp credential (and therefore which company) the request belongs to.',
    'card.be.notes.title': 'Service-Specific Notes',
    'tab.core.svc': 'sema-core-service',
    'tab.order.svc': 'sema-order-backend',
    'tab.bulk.svc': 'sema-bulk-backend',
    'be.core.desc': '<strong>sema-core-service</strong> — Main platform backend. Handles conversations, chats, scenarios, WhatsApp credentials, and template management. All resources are scoped to <code>credential_id</code>. Receives Kafka messages from <code>sema-router</code> on <code>CHAT_INCOMING_MESSAGE_TOPIC</code>.',
    'be.order.desc': '<strong>sema-order-backend</strong> — Order management. Same JWT + HMAC pattern. Resources scoped by credential. No Kafka consumption — communicates synchronously with core service via REST.',
    'be.bulk.desc': '<strong>sema-bulk-backend</strong> — Bulk campaign engine. On the <em>first</em> API call for a new <code>x-credential-id</code>, it calls the SSO API gateway to fetch the credential record and auto-provisions a <code>BulkTenant</code> in its own database. No manual setup needed per credential.',
    'card.be.hmac.title': 'Gateway Middleware Verification',
    'be.hmac.desc': 'Beyond JWT validation, backends verify the HMAC signature added by the gateway to ensure requests didn\'t bypass the gateway:',

    /* router */
    'page.router.title': 'sema-router (Go)',
    'page.router.desc': 'The WhatsApp webhook processor. It does not validate JWTs — it fetches credentials from the SSO API by <code>phone_number_id</code>.',
    'card.router.auth.title': 'How sema-router authenticates',
    'router.auth.info': '<strong>Different from all other services:</strong> sema-router receives WhatsApp webhooks from Meta — not requests from authenticated users. So there is no user JWT to validate.',
    'router.1.title': 'Meta webhook arrives',
    'router.1.desc': 'POST to <code>/webhook</code> from Meta\'s servers. Router verifies the webhook signature using the WhatsApp App Secret.',
    'router.2.title': 'Extract phone_number_id',
    'router.2.desc': 'From the webhook payload\'s <code>entry[0].changes[0].value.metadata.phone_number_id</code>.',
    'router.3.title': 'Fetch credential from SSO API',
    'router.3.desc': 'Calls <code>{SSOAPIBaseURL}/credentials?phone_number_id=&lt;id&gt;</code> to get the full credential record including <code>accessToken</code>, <code>credential_id</code>, <code>company_id</code>, etc.',
    'router.4.title': 'Process message + publish to Kafka',
    'router.4.desc': 'Runs scenario/AI logic using the credential. Publishes results to <code>CHAT_INCOMING_MESSAGE_TOPIC</code> with <code>credential_id</code> and <code>company_id</code> in the payload.',
    'card.router.env.title': 'Key Environment Variables',
    'card.router.kafka.title': 'Kafka Topics Published',
    'th.topic': 'Topic',
    'th.consumer': 'Consumer',
    'th.purpose': 'Purpose',
    'kafka.chat.desc': 'Bot/user message to persist and emit via WebSocket',
    'kafka.conv.desc': 'New conversation started',
    'kafka.typing.desc': 'WhatsApp typing indicator event',

    /* env vars */
    'page.envvars.title': 'Environment Variables Reference',
    'page.envvars.desc': 'Complete list of all required environment variables per service.',
    'card.env.gateway.title': 'SSO API Gateway (.env)',
    'card.env.frontend.title': 'Frontend Apps (Next.js)',
    'card.env.backend.title': 'NestJS Backend Services',
    'card.env.router.title': 'sema-router (Go)',
    'env.warn': '<strong>Secret synchronization:</strong> <code>GATEWAY_TOKEN_SECRET</code> and <code>GATEWAY_SHARED_SECRET</code> must be identical across the gateway and ALL NestJS backends. A mismatch causes all API calls to fail with 401.',
    'env.copy.all': 'Copy all',

    /* new app */
    'page.newapp.title': 'New App Integration Guide',
    'page.newapp.desc': 'Step-by-step: connect a brand new app to Sema SSO in under 30 minutes.',
    'card.newapp.fe.title': 'For a New Frontend (Next.js)',
    'card.newapp.be.title': 'For a New Backend (NestJS)',
    'card.newapp.checklist.title': 'Quick Validation Checklist',
    'newapp.fe.1.title': 'Set environment variables',
    'newapp.fe.1.desc': 'Add <code>NEXT_PUBLIC_SEMA_SSO_URL</code> pointing to the gateway, and <code>NEXT_PUBLIC_API_URL</code> for your backend.',
    'newapp.fe.2.title': 'Copy AuthProvider + useAuth from an existing app',
    'newapp.fe.2.desc': 'These are already battle-tested across 4 apps. Copy <code>providers/AuthProvider.tsx</code>, <code>hooks/useAuth.ts</code>, and <code>lib/httpClient.ts</code>. Do not reinvent this.',
    'newapp.fe.3.title': 'Wrap root layout with &lt;AuthProvider&gt;',
    'newapp.fe.3.desc': 'In <code>app/layout.tsx</code>, wrap children with <code>&lt;AuthProvider&gt;&lt;QueryClientProvider&gt;{children}&lt;/QueryClientProvider&gt;&lt;/AuthProvider&gt;</code>.',
    'newapp.fe.4.title': 'Create login page',
    'newapp.fe.4.desc': 'On the login page, redirect to <code>{SSO_URL}/auth/login?redirect_uri={window.location.origin}&amp;callback_mode=direct</code>. On page load, read <code>?token=</code> from URL and store it.',
    'newapp.fe.5.title': 'Use httpClient for all API calls',
    'newapp.fe.5.desc': 'The pre-configured <code>httpClient</code> attaches the <code>Authorization</code> header and handles token refresh automatically. Never call <code>fetch</code> directly for authenticated endpoints.',
    'newapp.be.1.title': 'Add environment variables',
    'newapp.be.1.desc': 'Add <code>GATEWAY_TOKEN_SECRET</code>, <code>GATEWAY_SHARED_SECRET</code>, <code>SERVICE_ID</code>, <code>REDIS_HOST</code>, <code>REDIS_PORT</code> to your <code>.env</code>.',
    'newapp.be.2.title': 'Install dependencies',
    'newapp.be.2.desc': '<code>@nestjs/jwt</code>, <code>@nestjs/passport</code>, <code>passport-jwt</code>, <code>ioredis</code>',
    'newapp.be.3.title': 'Register JwtModule',
    'newapp.be.3.desc': 'In your app module: <code>JwtModule.register({ secret: process.env.GATEWAY_TOKEN_SECRET, signOptions: { expiresIn: \'1h\' } })</code>',
    'newapp.be.4.title': 'Create JwtStrategy',
    'newapp.be.4.desc': 'Extend <code>PassportStrategy(Strategy)</code>. In <code>validate()</code>: check <code>iss === "api-gateway"</code>, check <code>aud.includes(SERVICE_ID)</code>, check Redis JTI blacklist.',
    'newapp.be.5.title': 'Add GatewayMiddleware',
    'newapp.be.5.desc': 'Create a middleware that validates <code>x-gateway-timestamp</code> and <code>x-gateway-signature</code> headers. Apply globally in <code>AppModule</code>.',
    'newapp.be.6.title': 'Tell the gateway about your audience',
    'newapp.be.6.desc': 'The gateway includes your <code>SERVICE_ID</code> in the <code>aud</code> claim when proxying to your service. Coordinate with the team to add your service ID to the gateway\'s audience config.',
    'newapp.check.1': 'Gateway returns <code>{ authenticated: true }</code> from <code>/auth/check</code>',
    'newapp.check.2': '<code>/auth/session</code> returns user object and token',
    'newapp.check.3': 'API call with token returns data (not 401)',
    'newapp.check.4': 'API call without token returns 401',
    'newapp.check.5': 'API call with expired token triggers refresh + retry',
    'newapp.check.6': 'Logout destroys session and redirects correctly',
    'newapp.check.7': '<code>GATEWAY_TOKEN_SECRET</code> is identical in gateway and your backend',
    'newapp.check.8': '<code>GATEWAY_SHARED_SECRET</code> is identical in gateway and your backend',
    'newapp.check.9': '<code>SERVICE_ID</code> is in the <code>aud</code> claim of tokens from the gateway',

    /* token flow */
    'page.tokenflow.title': 'Token Flow Diagrams',
    'page.tokenflow.desc': 'Detailed sequence diagrams for the most important flows.',
    'card.tf.login.title': 'Initial Login Flow',
    'card.tf.api.title': 'Authenticated API Call Flow',
    'card.tf.refresh.title': 'Token Refresh Flow',
    'card.tf.logout.title': 'Logout Flow',
    'card.tf.router.title': 'sema-router WhatsApp Webhook Flow',

    /* shared */
    'req': 'REQUIRED',
    'opt': 'optional',
    'copy': 'Copy',
    'copied': 'Copied!',
    'back_to_top': 'Back to top',
  }
};
